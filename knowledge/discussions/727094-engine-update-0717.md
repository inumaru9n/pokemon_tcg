# 727094: Updated game engine, sample submission file（運営、2026-07-17）

## 3行サマリ

- 運営が07-15/17にエンジンとsample submissionをマイナー更新（バグ修正）。配布データセット全体が07-15付で再アップロードされた
- 全ファイルdiffの結果、実差分はエンジンソース3ファイル+バイナリ4種のみ（SDK Python側・カードデータ・sample main.py/deck.csvは無変更）
- **07-17以降の本番は修正版エンジン、我々のローカルは07-24まで旧版**で評価していた（同期・再ビルド済み、下記）

## 取れる情報

- ★ **修正1（EffectProc.h）: Team Rocket専用エネルギー規則の適用先バグ** — 「非TRポケモンに付いたTR専用エネを破棄」する処理が、対象プレイヤー`i`でなく**常に`activePlayerIndex()`のエネ**を破棄していた（ループ変数シャドーイング）。**Spidops（TRエネ使用、メタ13.1%）が絡む対戦の挙動が新旧で異なる**
- ★ **修正2（State.h）: tool位置解決バグ** — `attachedToolPositionFromOption` が `ps.tool` の列挙で `ps.energy[i]` を読んでいた。Tool Scrapper系のtool選択解決に影響
- 修正3（BattleData.h）: アクション数3000での強制引き分けを撤廃（6/30の本番挙動変更のソース反映。ローカルarenaはstep_capで従来通り保護）
- **提出物への影響**: 同梱cgのlibcgは**agentプロセス内のsearch API実行に使われる**ため、旧cg同梱の提出（07-22の058再提出含む）はlethal探索のシミュレーションが本番実挙動と微妙にずれる。致命的ではない（探索はprz≤3の自ターン内でTRエネ/tool分岐は稀）が、**次回提出から新cgを同梱すべき**（配布dirを同期済みなのでpackage_submission.pyは自動で新版を拾う）

## 対応済み（2026-07-24）

1. 配布dir（pokemon-tcg-ai-battle/data/）のエンジンソース3ファイル+sample cgバイナリ4種を新版に同期（旧版は dist_backup_pre0715/ に保存）
2. cg/libcg.dylib を新ソース+Export_seeded.cpp（CRNパッチ）で再ビルド、決定性バイト一致を再検証（PASS）
3. 回帰スポットチェック: 083 Spidopsゲート（TRエネ影響最大）+075プールv4値を新エンジンで再測定

## 回帰チェック結果（2026-07-24、影響なしと確定）

- **同一シードでの新旧エンジン直接比較（075 vs 082、400戦）: 399/400ゲームが完全一致**、差分+0.25pt±0.5。修正コードパス（TRエネ誤適用・tool解決）は主要対面でほぼ発火しない
- 083 Spidopsゲート: 92.5% vs 旧92.8%＝不変（Spidops方策はTRエネをTRポケモンに正しく貼るため規則発火が稀）
- 075プールv4の見かけの低下（60.1→55.3）は独立n=200同士のサンプリングノイズと確定（CRN比較が2σ変動を1時間で切り分けた実例）。**旧エンジンでの直近評価（EXP-079〜087・プールv4）は全て有効**

## 注意点・疑問

- アナウンスの「the bug identified here」のリンク先スレッドは未特定（本文にURLが残っていない）
- n=200/対面のプール値は対面あたり±7ptのノイズを持つ。重要な意思決定はCRNペアで（確立済み手順）
