"""Critic用の状態符号化の仕様（次元定数のみ。依存なし）。

`state_encoder.py`（生成側・cg依存）と `value_net.py`（学習側・torch依存）が
**同じ定数を見る**ために切り出した。ローカルにtorchが無く、Kaggleにcgが無い、という
環境の非対称があるので、両者が共有できるのはここだけになる。

ゾーンの並び（変えると学習済みCriticが無効になる）:
  0 = 手札
  1 = 山∪サイド（どちらも相手が今後引きうる未公開の集合。価値評価上は区別が薄い）
  2 = 場（アクティブ+ベンチ、および付いているエネ/ツール/進化元）
  3 = トラッシュ
"""

N_ZONE = 4
ZONE_CAP = 64      # 1ゾーンの最大カード数（デッキ60 + 余裕）
N_ATTR = 14        # state_encoder.attrs() の次元
N_PROG = 8         # state_encoder.encode() が返す進行情報の次元
CARD_ID_OFFSET = 1  # 埋め込み索引 = cardId + 1（0をpaddingに使うため）
DEFAULT_N_CARDS = 2048  # カードID最大値+余裕。IDをそのまま索引に使うので語彙不要

# **未知カードのトークン**。相手の隠れ領域を伏せるときに使う。
# ゼロ埋め（padding）は「空」を意味してしまい、相手の山札が空＝山札切れ＝こちらの勝ち、
# という**偽の手掛かり**になる（実測: ターン20以降で平均予測0.601 vs 実勝率0.101、
# AUC 0.203と逆転した）。「N枚の未知のカードがある」を表すには専用トークンが要る。
UNKNOWN_CARD = 2047
